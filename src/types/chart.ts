type ChartData = {
    date: string;
    count: number;
};

type HazardChartType = {
    name: string;
    value: number;
};

type HistogramType = {
    name: string;
    count: number;
};

export { ChartData, HazardChartType, HistogramType };
